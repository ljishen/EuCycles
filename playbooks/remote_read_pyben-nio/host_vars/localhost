---
transmission_sizes: ['10%', '20%', '30%', '40%', '50%', '60%', '70%', '80%', '90%']
repetition: 3

# This value has nothing to do with the 'server_data_file_size'.
# It could be bigger or smaller than the value of 'server_data_file_size'.
dataset_size: '1G'

# Don't change this option except you know what you are doing!
backup_results: yes

result_filename: result.json

server_outbound_interface: br_flat_lan_1
client_incoming_interface: br_flat_lan_1

# These are the options for Pyben-nio.
# See the description page for more details
# https://github.com/ljishen/pyben-nio
port: 8881
server_bufsize: 1024mb
client_bufsize: 128kb
server_filtering_method: "match; func=lambda v: int(chr(v)) <= INDEX; sztype=BEFORE"
client_filtering_method: "raw"

# See the doc for option "--cpuset-cpus" on the container
# https://docs.docker.com/engine/reference/run/#cpuset-constraint
# Omit this variable to skip the cpuset constraint
perf_server_container_cpuset:
perf_client_container_cpuset_start: 4

# Use auto to programmatically match the size of the cpuset
# range equals to the number of servers
perf_client_container_cpuset_end: auto
