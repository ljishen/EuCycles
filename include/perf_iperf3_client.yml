---
- name: ensure the presence of dir logs
  local_action:
    module: file
    path: "{{ workdir }}/logs"
    state: directory
  run_once: true
- name: start tests for transmission size {{ bytes }}
  include_tasks: include/single_test.yml
  with_sequence: count={{ repetition }}
  loop_control:
    loop_var: index
